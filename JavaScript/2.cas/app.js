// JS Output
// Postoji nekoliko nacina da prikazemo podatke u JS:l
// 1. innerHTML
// Primer:
document.getElementById('paragraf').innerHTML = 'Paragraf dodat preko JS';

// 2. document.write() - Koristi se za testiranje koda.

// 3. alert() - salje nam upozorenje
// Primjer:
// alert('Pozdrav');

// 4. console.log() - najcesce koriscena funkcija za prikazivanje stvari u konsoli.
// Primer:
console.log('Pozdrav iz konzole');

// JS Identifers/Names
// JS names(imena varijabli) moraju pocinjatisa:
// 1. slovom;
// 2. dollar sighn ($);
// 3. underscore (_)
// Napomena: promenjiva ne moze pocinjati brojevima ali se mgu naci kasnije.

// JS is case sensitive!!! (A i a nije isto! )

//              JS varijable

// Postoji nekoliko nacina da deklarisemo varijablu:

// 1. Koriscenjem var kljucne rijeci (keyword)
var x; //deklaracija
x = 10; // dodjela vrijednosti(inicijalizacija)
var x = 10; // odjednom izvrsena deklaracija i inacijalizacija

// 2. Koriscenjem let kljucne rijeci (keyword)
let x;
x = 10;
let x = 10;

// 3. Koriscenjem const kljucne rijeci (keyword)
const x = 10; //Kod const mora odjednom.

// Varijable (promenjive) su kontejneri za skladistenje vrijednosti.

